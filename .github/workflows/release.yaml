name: "Manual release"

on:
  push:

jobs:
  release:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: ["dev", "int", "prd"]
    env:
      ENV: env-is-${{ matrix.env.uppercase }}
    steps:
      - run: |
          echo $ENV
