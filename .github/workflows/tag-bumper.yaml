name: Manipulate YAML values
on:
  push:
    branches:
      - main
#  repository_dispatch:
#    types: [my-event]
jobs:
  myEvent:
    runs-on: self-hosted
    container:
      image: mikefarah/yq
    steps:
      - name: Run PWD
        run: |
          echo $GITHUB_WORKSPACE
          cat values.yaml

      - uses: actions/checkout@v2
