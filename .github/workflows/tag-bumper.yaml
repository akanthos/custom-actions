name: Manipulate YAML values
on:
  push:
    branches:
      - main
#  repository_dispatch:
#    types: [my-event]
jobs:
  myEvent:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2

      - name: Run PWD
        run: |
          echo $GITHUB_WORKSPACE
          cat values.yaml
