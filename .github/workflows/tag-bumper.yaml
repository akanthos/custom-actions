name: Manipulate YAML values
on:
  push:
    branches:
      - main
#  repository_dispatch:
#    types: [my-event]
jobs:
  myEvent:
    runs-on: self-hosted
    container:
      image: mikefarah/yq
    steps:
      - name: Run PWD
        run: |
          echo $USER
          whoami
          groups

#      - uses: actions/checkout@v2
