name: "Triggers check"

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'foo/**'
      - 'bar/**'
      - '**/terraform-dhc.yaml'

jobs:
  plan:
    name: "Plan"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
